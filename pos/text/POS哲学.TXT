



- POS 是一种结构体的扩充, 它是一种以实践为导向的灵魂和实现工具,
  而不一定必须是纯粹的结构体.

- POS 是一种内存布局, 在这一点上它的重要性就像接口中的虚表.

- POS 的最高形式即是 TIT, TIT 的最高境界即是泛g.

- POS 就好比 STL 的原型与实现.

- POS


// convert flow graph

	// 状态数 N
	// 已经定义的转换数目 T
	// 转换中间步骤限制 M
	// 求：转换表的大小 S
	//
	// S = (T/2)^M
	//	T 可能在 100-500 左右，因此转换表大小 250k * sizeof(element) 约 2M 左右。


//
