

HAS单元属性


	- 另一种提高开发效率的方法


当然可以在一个函数的两端编写一些辅助代码：
	{
		参数检查
		环境确认
		准备工作
		……
		析构
		检验
	}
有些代码则可以用属性代替，比如对于某些参数，检查其是否溢出、越界、无效，或者检查环境是否有效等等可以用属性表示。单元的运行安全属性中可以指定是否进行溢出、越界、无效检查，而完整性属性中可以指定是否进行环境确认检查。
