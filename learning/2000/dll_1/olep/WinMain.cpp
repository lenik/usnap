
#include "stdafx.h"
/*
#define	INITGUID
#include "appl.h"

Application	thaApp;

int PASCAL WinMain(HINSTANCE hinstCurrent, HINSTANCE hinstPrevious, LPSTR lpszCmdLine, int nCmdShow) {
	if (hinstPrevious != NULL) {
		MessageBox(
			NULL,
			"This application is already running!",
			"",
			MB_OK | MB_ICONINFORMATION);
		return FALSE;
	}

	theApp.m_hInst		= hinstCurrent;
	theApp.m_hInstPrev	= hinstPrevious;
	theApp.m_nCmdShow	= nCmdShow;
	theApp.m_lpszCmdLine	= lpszCmdLine;

	if (!theApp.Initialize()) return FALSE;

	int status	= theApp.Run();
	theApp.deInitialize();

	return status;
}
*/