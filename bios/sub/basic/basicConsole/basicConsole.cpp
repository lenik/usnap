// basicConsole.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"

/*
// 使用定时器检测信息的到来
void CALLBACK mytimer(HWND hwnd, UINT uMsg, UINT idEvent, DWORD dwTime) {
	MessageBox(NULL, "Hello", NULL, 0);
}

int main(int argc, char* argv[])
{
	SetTimer(NULL, 0, 2000, &mytimer);
	MessageBox(NULL, "main", NULL, 0);
	return 0;
}
*/

int main(int argc, char* argv[])
{
}
